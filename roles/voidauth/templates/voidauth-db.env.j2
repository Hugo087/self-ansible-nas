# Généré par Ansible - ne pas éditer à la main (roles/voidauth/templates/voidauth-db.env.j2)
# Déployé en mode 0600 (voir roles/voidauth/tasks/main.yml) : contrairement à un
# Environment= en dur dans le quadlet (lisible par tout le système via le
# .container en 0644), ce fichier n'est lisible que par {{ sso_user }} et root.
POSTGRES_PASSWORD={{ db_password }}
